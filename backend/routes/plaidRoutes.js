/*

import express from "express";
import {
  createLinkToken,
  exchangePublicToken,
  itemRemove,
} from "../controllers/plaidController.js";

// Router helps map API functions to their corresponding routes (URLs)
const router = express.Router();

router.post("/create_link_token", createLinkToken);
router.post("/exchange_public_token", exchangePublicToken);
router.post("/remove_item", itemRemove);

export default router;

*/